//! glyph-cli — Port of `scripts/glyph-cli.js` — flag for flag (M11).

fn main() {
    eprintln!("glyph (rust) {}: not ported yet — use node scripts/glyph-cli.js", env!("CARGO_PKG_VERSION"));
    std::process::exit(2);
}
