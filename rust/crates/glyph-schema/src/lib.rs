//! glyph-schema — Port of `scripts/glyph-check.js` — validates a `GlyphAST` envelope against `.guidelines/ast-schema.json`.
//! Imports nothing from the core, on purpose: the receiving end may have the
//! store and not the engine.
