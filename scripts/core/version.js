/**
 * core/version.js — the engine's version, in one place.
 *
 * The changelog moves this number (`.guidelines/.changelog/README.md`); the
 * entry exports it, the skill builder stamps it, and the inverse compares an
 * incoming document's `engine=` against it. release.frontend.rules.minor —
 * no digit resets another.
 */
export var VERSION = "3.5.8.06";
