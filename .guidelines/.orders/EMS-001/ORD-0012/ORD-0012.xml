<glyph-package engine="3.5.8.06">
  <schema/>
  <block once="true">
    <block binds="ORD-0012-cli">
      <user-input>ORD-0012-cli</user-input>
      <context>
        <user-input>the spec of EMS-001</user-input>
        <user-input>.guidelines/.orders/EMS-001/EMS-001.pgml</user-input>
      </context>
      <only-if>
        <user-input>ORD-0009 closed</user-input>
      </only-if>
      <target>
        <user-input>glyph-bundle and flag parity with scripts/glyph-cli.js</user-input>
      </target>
      <constraint>
        <user-input>SOURCE_DATE_EPOCH, when set, is the moment the manifest names as emitted and the zip carries, and the time of the zip is UTC in both engines; the JS first, as question 16 of the return answers it</user-input>
      </constraint>
      <validate>
        <invoke reads="[CMP-CTX[CNST]][SUB[EQ[CMP-CTX]]][CAT-EQ]" species="composite" depth="5"/>
        <user-input>the Rust CLI</user-input>
        <user-input>each flag answers the same bytes: --ast --xml --diag --expand --hgml --from-xml --file --bundle --out</user-input>
      </validate>
    </block>
  </block>
</glyph-package>