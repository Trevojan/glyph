<!-- GENERATED by build-skill.js from conformance/examples.json at engine 2.4.5.02 — DO NOT EDIT BY HAND.
     Edit the source in the glyph repository and run: node scripts/build-skill.js -->

# Examples — perishable

Produced by engine **2.4.5.02** from sources authored on 2026-08-30.

These illustrate; they do not define. `../XML_REFERENCE.md` is normative and
outranks anything here. An example is bound to one input and one engine
version, so it goes stale in a way the reference does not — if the two ever
disagree, the reference is right and this file needs regenerating.

---

## E-01

```glyph
[in
  [tgt`phonema-orbital`]
  [rtnl-go`cover cases where 2 vowels cost -0.1 when repeated`]
  [nt`start`]
  [ctx-ref`now: aa -> 0.9 + 0.9 | expected: aa -> 0.9 + 0.8`]
  [alw-true`alphanumeric inputs in the app's CLI.`]
  [nt`path`]
  [instof-add`no need to change anything besides adding this rule. try to change the code as less as possible.`]]
```

```xml
<glyph-package engine="2.4.5.02">
  <schema/>
  <block once="true">
    <instruction>
      <target>
        <user-input>phonema-orbital</user-input>
      </target>
      <rationale>
        <invoke reads="[ELAB[RSN]],[REF[CNST]]" species="composite" depth="1"/>
        <chain>
          <go/>
        </chain>
        <user-input>cover cases where 2 vowels cost -0.1 when repeated</user-input>
      </rationale>
      <note>
        <user-input>start</user-input>
      </note>
      <context>
        <chain>
          <reference/>
        </chain>
        <user-input>now: aa -&gt; 0.9 + 0.9 | expected: aa -&gt; 0.9 + 0.8</user-input>
      </context>
      <always force="editorial">
        <chain>
          <true/>
        </chain>
        <user-input>alphanumeric inputs in the app's CLI.</user-input>
      </always>
      <note>
        <user-input>path</user-input>
      </note>
      <instead-of>
        <chain>
          <add/>
        </chain>
        <user-input>no need to change anything besides adding this rule. try to change the code as less as possible.</user-input>
      </instead-of>
    </instruction>
  </block>
</glyph-package>
```

> Does not survive `fromXML`, by design — see `roundTripNote` in the vector.

---

## E-02

```glyph
[in
  [tgt`combat-system`]
  [rtnl-go`determine whether damage should be applied before armor reduction`]
  [cond-if`the attack is physical`]
  [alw-true`only when the attacker has a valid target`]
  [instof-add`keep the existing damage pipeline intact.`]]
```

```xml
<glyph-package engine="2.4.5.02">
  <schema/>
  <block once="true">
    <instruction>
      <target>
        <user-input>combat-system</user-input>
      </target>
      <rationale>
        <invoke reads="[ELAB[RSN]],[REF[CNST]]" species="composite" depth="1"/>
        <chain>
          <go/>
        </chain>
        <user-input>determine whether damage should be applied before armor reduction</user-input>
      </rationale>
      <condition>
        <chain>
          <if/>
        </chain>
        <user-input>the attack is physical</user-input>
      </condition>
      <always force="editorial">
        <chain>
          <true/>
        </chain>
        <user-input>only when the attacker has a valid target</user-input>
      </always>
      <instead-of>
        <chain>
          <add/>
        </chain>
        <user-input>keep the existing damage pipeline intact.</user-input>
      </instead-of>
    </instruction>
  </block>
</glyph-package>
```

---

## E-03

```glyph
[in
  [tgt`TurnService.Resolve`]
  [rev-cmp`compare the current resolution order against the expected combat order`]
  [ctx-ref`current: defense resolves before attack | expected: attack resolves before defense`]
  [prop`identify the smallest correction necessary`]
  [instof-add`do not redesign TurnService.`]]
```

```xml
<glyph-package engine="2.4.5.02">
  <schema/>
  <block once="true">
    <instruction>
      <target>
        <user-input>TurnService.Resolve</user-input>
      </target>
      <review>
        <chain>
          <compare/>
        </chain>
        <user-input>compare the current resolution order against the expected combat order</user-input>
      </review>
      <context>
        <chain>
          <reference/>
        </chain>
        <user-input>current: defense resolves before attack | expected: attack resolves before defense</user-input>
      </context>
      <propose>
        <invoke reads="[GO[ALT[RTNL]]],[ASK[CONF]]" species="composite" depth="2"/>
        <user-input>identify the smallest correction necessary</user-input>
      </propose>
      <instead-of>
        <chain>
          <add/>
        </chain>
        <user-input>do not redesign TurnService.</user-input>
      </instead-of>
    </instruction>
  </block>
</glyph-package>
```

---

## E-04

```glyph
[in
  [tgt`Player.Lives`]
  [ask-clar`is an unrevealed Life always worth 2 points?`]
  [ctx-ref`current scoring rule`]
  [conf`required before implementation`]]
```

```xml
<glyph-package engine="2.4.5.02">
  <schema/>
  <block once="true">
    <instruction>
      <target>
        <user-input>Player.Lives</user-input>
      </target>
      <ask>
        <chain>
          <clarification/>
        </chain>
        <user-input>is an unrevealed Life always worth 2 points?</user-input>
      </ask>
      <context>
        <chain>
          <reference/>
        </chain>
        <user-input>current scoring rule</user-input>
      </context>
      <confirmation>
        <user-input>required before implementation</user-input>
      </confirmation>
    </instruction>
  </block>
</glyph-package>
```

---

## E-05

```glyph
[in
  [tgt`input-parser`]
  [lim`do not modify tokenization`]
  [req`the existing syntax must remain valid`]
  [mand`the new rule must apply to CLI input`]
  [avd`avoid introducing a new parser stage`]
  [dont`change unrelated commands`]]
```

```xml
<glyph-package engine="2.4.5.02">
  <schema/>
  <block once="true">
    <instruction>
      <target>
        <user-input>input-parser</user-input>
      </target>
      <limitation>
        <user-input>do not modify tokenization</user-input>
      </limitation>
      <requirement>
        <user-input>the existing syntax must remain valid</user-input>
      </requirement>
      <mandatory>
        <user-input>the new rule must apply to CLI input</user-input>
      </mandatory>
      <avoid>
        <user-input>avoid introducing a new parser stage</user-input>
      </avoid>
      <do-not>
        <user-input>change unrelated commands</user-input>
      </do-not>
    </instruction>
  </block>
</glyph-package>
```
